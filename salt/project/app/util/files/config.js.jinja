{%- set util_compound_string = "G@node_type:util and G@datacenter:" +  grains['datacenter'] + " and G@env:" + grains['env'] -%}
var config = new Settings(
{
  elasticsearch:    "http://{{ salt['publish.publish']( util_compound_string, 'network.ip_addrs', 'eth0', expr_form='compound').values()[0][0] }}:9200",
  kibana_index:     "kibana-int",
  modules:          ['histogram','map','pie','table','filtering',
                    'timepicker','text','fields','hits','dashcontrol',
                    'column','derivequeries','trends','bettermap','query',
                    'terms'],
  }
);